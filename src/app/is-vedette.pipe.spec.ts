import { IsVedettePipe } from './is-vedette.pipe';

describe('IsVedettePipe', () => {
  it('create an instance', () => {
    const pipe = new IsVedettePipe();
    expect(pipe).toBeTruthy();
  });
});
